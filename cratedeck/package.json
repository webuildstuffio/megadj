{
  "name": "cratedeck",
  "private": true,
  "version": "0.2.0",
  "description": "Local dashboard for every DJ USB drive — mounted or not",
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "web:dev": "cd web && bun run dev",
    "web:build": "cd web && bun run build",
    "start": "bun run src/index.ts",
    "test": "bun test ../cratedeck/test ../cratedeck/shared"
  }
}
