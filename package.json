{
  "name": "megadj",
  "version": "0.1.0",
  "description": "Rate-limited YouTube Music library archiver with rekordbox-ready metadata",
  "type": "module",
  "module": "src/cli.ts",
  "bin": {
    "megadj": "./src/cli.ts"
  },
  "workspaces": [
    "cratedeck"
  ],
  "scripts": {
    "megadj": "bun run src/cli.ts",
    "deck": "bun run cratedeck/src/index.ts",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint src cratedeck/src cratedeck/shared cratedeck/web cratedeck/test",
    "check": "bun run typecheck && bun run lint",
    "test": "bun test"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "@types/bun": "^1.3.0"
  }
}
