---
name: metrics-weekly
"on":
  schedule:
    - cron: "0 13 * * 0" # Sundays 09:00 ET (re-registered)
  release:
    types: [published]
  workflow_dispatch:

jobs:
  snapshot:
    uses: webuildstuffio/repo-review/.github/workflows/metrics-reusable.yml@main
    with:
      run_snapshot: true
      language: typescript
    secrets: inherit
