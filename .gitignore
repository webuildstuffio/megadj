# Dependencies, builds, and coverage
node_modules/
dist/
coverage/
htmlcov/
*.tsbuildinfo
.coverage
.coverage.*

# Python environments and tool caches
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Local agent, search, and code-graph state
.cursor/
.tokensave/
.megamem/
# Legacy MegaMem layout; current indexes live in .megamem/
workspace-megadj/
.local-screens/

# Runtime databases and private configuration
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite-wal
*.sqlite-shm
src/deck/data/
src/deck/data-test-*/
src/deck/config.toml
config.toml
.env
.env.*
!.env.example

# Logs, scratch files, backups, and generated reports
*.log
nohup.out
*.part
*.tmp
*.bak
*.backup
*.orig
reports/
docs/usb-sync-log.md
/artifacts/
/outputs/
/logs/
/tmp/
/temp/
/embeddings/

# Experiment definitions/config stay tracked; only generated outputs above do.

# Raw media belongs in the configured archive, never in Git
*.aac
*.aif
*.aiff
*.alac
*.flac
*.m4a
*.m4b
*.mp3
*.mp4
*.ogg
*.opus
*.wav
*.webm

# Local quarantine evidence; archive quarantine itself belongs on the shelf
.archive-quarantine/

# OS metadata
.DS_Store
.AppleDouble
._*
.Spotlight-V100/
.Trashes/
